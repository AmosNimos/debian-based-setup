sudo apt install awesome
#tiling window manager

sudo apt install youtube-dl
#to download youtube video

sudo apt install tmux
#Additional tip: you can copy content from w3m with ctrl-b [ ctrl-space ctrl-w and past it to other tmux panel with ctrl-b ]

sudo apt install w3m
#Additional tip: you can open w3m images with shift-i

sudo apt install ranger
#Additional tip: you can use CDP https://github.com/AmosNimos/change-directory-plus instead 

sudo apt install calibre
#help read and convert pdf and epub files

sudo apt install gimp
#to edit pictures

sudo apt install jp2a
#convert images to text images

sudo apt install rofi
#application luncher

sudo apt install gnome-tweak-tool
#custumise gui application default theme.

sudo apt install xdotool
#for auto-fill bash

sudo apt install nethack-console
#a terminal game because we all need to relax some time.
